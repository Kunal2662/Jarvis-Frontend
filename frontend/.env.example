# JARVIS frontend environment — copy to `.env` and fill in.
# `.env` is gitignored. Only VITE_* vars are exposed to the browser bundle.

# Backend base URL. Leave empty for same-origin, or point at the local API.
VITE_BACKEND_URL=http://localhost:8000
